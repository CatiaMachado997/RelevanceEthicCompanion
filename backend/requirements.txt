# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
# Required by pydantic EmailStr (used in auth/user models).
email-validator>=2.2.0

# Database & Storage
# psycopg3 for Python 3.13 compatibility
psycopg[binary,pool]>=3.2.0
pgvector==0.2.4

# Vector DB (V2)
weaviate-client>=4.4.0

# LLM & AI - V1 (being phased out)
groq>=0.9.0

# LLM & AI - V2 (new)
google-genai>=1.0.0
langchain>=0.1.0
langchain-groq>=0.3.3
langchain-google-genai>=0.0.5
openai==1.7.2  # For embeddings if needed

# Search & External APIs
tavily-python>=0.5.0

# Google Cloud Services
google-cloud-texttospeech==2.16.0
google-auth==2.27.0

# Google APIs (V2 - Calendar integration)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.4.0
google-cloud-secret-manager>=2.16.0

# Scheduling (V2)
apscheduler>=3.10.0

# Document processing
pypdf>=4.0.0
python-docx>=1.1.0

# Utilities
python-dotenv==1.2.2
python-jose[cryptography]>=3.3.0
cryptography>=46.0.5
pyasn1>=0.6.2
PyJWT>=2.12.0
python-multipart>=0.0.22

# Testing
# pytest 9.0.3 fixes GHSA tmpdir handling (medium).
# pytest-asyncio 1.3.0+ is required for pytest 9.x compatibility
# (pytest-asyncio 0.23 raises AttributeError: 'Package' object has no attribute 'obj').
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov==4.1.0

# Rate Limiting
slowapi==0.1.9

# Development
black==26.3.1  # fixes GHSA / CVE-2024-21503 (arbitrary file writes via --python-cell-magics)
flake8==7.0.0
mypy==1.8.0

# Type stubs
types-requests>=2.31.0

# Sprint 2a
langgraph>=0.2.0
fastapi-mcp>=0.4.0
# Pinned to 2.x line — langfuse 3.x+ pulls opentelemetry-api which causes pip resolution-too-deep.
# The app only uses Langfuse(public_key, secret_key, host), a stable API across 2.x/3.x.
langfuse>=2.60.0,<3.0
anthropic>=0.25.0
mcp

# Tool Marketplace — Composio managed integrations
composio-langchain==0.11.4
